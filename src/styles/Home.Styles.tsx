import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  listContainer: {
    width: '100%',
  },
  pokeball: {
    position: 'absolute',
    top: -50,
    left: 100,
    transform: [{rotate: '50deg'}],
  },
});
